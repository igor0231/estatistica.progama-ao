# Carregando base de dados
dados <- mtcars

srt (dados)
View(dados)
dim(dados)
names(dados)

summary(dados)

media_mog <- mean (dados$mpg)
media_mpg

mediana_mpg <- median(dados$mpg)
mediana_mpg

media <- sapply(
  dados [variaveis_quantitativos],
  median

)

medias
medianas

sd_mpg <-sd(dados$mpg)
sd_mpg

var_mpg <- var(dados$mpg)
var_mpg

amplitude_mpg <- max(dados$mpg) - min(dados$mpg)
amplitude_mpg

n_mpg <- length(dados$mpg)
ep_mpg <-sd(dados$mpg) / sqrt(n_mpg)
ep_mpg

erros_padrao <-sapply(
  dados[variaveis_quantitativa]
  function(x) sd(x) / sqrt(length(x))
)
erros_padrao

cv_mpg <- (sd(dados$mpg) / mean(dados$mpg)) * 100
cv_mpg

#calculando o CV para todas as variaveis quantitativas
coeficientes_variacao <- sapply(
  dados[variaveis_quantitativa],
  function(x) (sd(x) / mean (x)) * 100
)

coeficientes_variacao

# Variaveis quantitativas continuas que serao analisadas
variaveis_quantitativas <- c(
  
  "mpg" , # milhas por galao (consumo de combustivel)
  "disp" , # cilindrada em polegadas cubicas
  "hp" , # pontecia em cavalos - vapor (horsepower)
  "drat" , # relacao do diferencial traseiro
  "wt" , # peso em mil libras (ex:2.62 = 2620 libras)
  "qsec" # tempo para percorrer 1/4 de milha em segundos
  
)

# Variaveis categoricas/discretas (serao usadas para agrupamento)
# cyl = nnumeros de cilindros (4, 6 ou 8) -> discreta/ordinal
# gear = numero de marchas (3, 4 ou 5) -> discreta/ordinal
# am = tipo de trasmissao (0=automatico, 1=manual) -> nominal
